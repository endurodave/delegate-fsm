#ifndef _STATE_MACHINE_HSM_TESTS_H
#define _STATE_MACHINE_HSM_TESTS_H

// Run all StateMachineHSM unit tests. Each test uses ASSERT_TRUE — any failure
// aborts via FaultHandler. Prints "All StateMachineHSM tests passed." on success.
void RunStateMachineHSMTests();

#endif // _STATE_MACHINE_HSM_TESTS_H
